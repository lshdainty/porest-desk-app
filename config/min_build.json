{
  "_": "이 빌드번호보다 낮은 앱은 강제로 업데이트해야 쓸 수 있다. 서버 스키마가 바뀌거나 옛 앱이 잘못된 값을 보내는 것 같은, 그냥 두면 안 되는 변경에만 올린다. 평소에는 건드리지 않는다 — 올리는 순간 그보다 낮은 모든 사용자가 앱을 못 쓴다.",
  "_howto": "GitHub Actions 의 실행 번호가 곧 buildNumber 다. 강제할 최소값을 여기에 적으면 CI 가 version.json 에 실어 보낸다. 릴리스 페이지나 앱 계정 화면에서 현재 번호를 확인할 수 있다.",
  "minBuildNumber": 0
}
